@tool
extends EditorPlugin
## Editor-side hook for the Shadow Glass addon. The runtime system is driven by
## ShadowGlassSystem nodes in the scene; the plugin itself only needs to be
## enabled so the addon folder is recognised.


func _enter_tree() -> void:
	pass


func _exit_tree() -> void:
	pass