欢迎回来，
前不久我做了一个视频，
试图弄清楚project shadow glass的视觉效果是如何实现的，
那时我已经知道了其背后的原理，
但仍然找不到在高德之中复现它的方法，
主要是因为GODOT管线的局限性，
后来我在开发自定义全局光照着色器时，
终于找到了方法，
今天我要解释我用来实现这个效果的方法，
它内部是如何工作的，
以及你如何自己实现类似的效果，
请记住，
这不是一个分步教程，
我只会提供这项技术的基础原理，
四个月前我问过开发者，
我的方向是否正确，
但从未得到回复，
经过数周的实验调试，
以及与合成器和计算着色器问题的斗争，
我的逆向工程方法终于成功了，
我把它做成了GODOT插件，
虽然我把它做成了付费插件，
但我仍然想在这里展示它的工作原理，
GODOT社区一直非常乐于助人，
我认为开发者之间的分享很重要，
如果你想看看这个插件有什么功能，
可以跳过这张，
否则请留下来了解这项技术，
正如我之前的视频所示，
将场景渲染到立方体贴图，
然后用相机采样可以消除旋转时的闪烁，
但在移动时仍然会有闪烁，
唯一能稳定移动的方法是完全停止移动，
相反我们在移动时对立方体贴图进行变形，
使其能够正确的投影到几何体上，
为此我们需要两个立方体贴图，
一个只进行捕捉，
另一个跟随相机静态立方体贴图在离相机太远时才会捕捉到相机位置，
在捕捉之前。
静态立方体贴图会在内存中存储一份自身的副本，
这个副本用于平滑D差值，
在之前和当前位置之间，
立方体贴图中存储了深度信息，
用于确定立方体贴图能看到什么和不能看到什么，
我用品红色高亮显示遮挡区域，
当我们移动时，
它会不断增长，
直到立方体贴图再次捕捉，
大部分颜色来自静态立方体贴图，
而品红色区域则回退到跟随相机的立方体贴图，
我们也可以回退到原始场景，
消除所有闪烁，
但代价是一些非像素化的遮挡区域会出现闪烁，
但闪烁并没有那么令人分心，
请注意，
我使用了抖动，
这大大放大了闪烁效果，
但立方体贴图通常不携带深度信息，
让我们看看如何在GODOT中解决这个问题，
在我之前的视频中，
我展示了如何使用六个子视口和六个相机来创建立方体贴图，
视口控制分辨率，
我还为立方体贴图创建了一个独立的场景，
现在每个相机需要一个合成器效果，
他的主要工作是将相机的镜像深度压缩，
并存储在立方体贴图像素的alpha通道中，
计算着色器存储深度，
并以保留进场精度的方式进行压缩，
我们不太关心原厂立方体贴图是否输出，
现在在其alpha通道中包含深度，
使用gd script，
我们捕获该输出，
并将其与立方体贴图的世界位置一起，
传递到面向相机的平面片源着色器上，
为了正确采样立方体贴图，
我们需要当前像素的世界位置和立方体贴图的中心。
由于立方体贴图使用视图方向进行采样，
我们只需要在片源着色器中相应地偏移该方向即可，
我们使用这个校正后的方向来采样立方体贴图，
并解码存储在alpha通道中的压缩深度，
现在我们只需要比较相机深度和立方体贴图深度，
如果差异太大，
它可能是采样到了一个被遮挡的区域，
这里我们将两个立方体贴图重新投影到世界空间，
拒绝无效的深度不匹配，
并在线性空间中混合有效采样，
以实现平滑的食欲过渡，
这就是核心技术，
这应该足够让你自己实现了，
但如果你想避免合成器和计算着色器的麻烦，
我将向你介绍我的插件，
下载后创建一个新的GODOT4.51plus项目，
确保它使用forest plus，
其他管线不会工作，
打开你的资源文件夹，
创建一个名为ADAMS的文件夹，
并打开它，
找到你的cube插件文件夹，
并将其复制到ADDANCE文件夹中，
你可能会遇到一些错误，
只需在项目项目设置插件中启用插件即可，
现在插件正在运行，
添加你的对象，
并设置你的场景，
正如你所见，
他开箱即用，
它带有一个像素化天空着色器，
只需将一个环境添加到场景中，
并将其用作你的天空，
着色器在materials文件夹中有多个变量可以调整，
还有一些你可以使用的纹理，
如大气和云也都在materials文件夹中，
我还包含了一个脚本来自动化昼夜周期，
你需要两个定向光源才能使用它，
我这里将跳过设置。
但你可以通过插件手册来了解天空盒上的像素密度是可配置的，
抖动光晕大小，
太阳大小等也是如此，
不胜枚举，
现在让我们来看看主要的Q币配置，
在插件文件夹中找到一个标记为q b saved config的图标，
并双击它，
检视器将弹出可用输入，
你可以在编辑器视口中启用或禁用效果，
随时更改分辨率，
更改立方贴图和相机之间的捕捉距离，
以及混合两个立方贴图所需的时间，
更改回退距离，
这样玩家持武器时就不会被裁剪，
禁用或启用天空盒，
在立方贴图上的渲染，
它会更像像素艺术，
但会在几何体与天空交汇处产生更多闪烁，
此外你还需要更改武器渲染所在的图层嗯，
让我们稍微改变一下环境，
改变环境影响的扩散范围，
现在让我们让它回退到立方贴图，
而不是原始场景，
它在边缘会有更多闪烁，
但会更像像素艺术，
取消勾选它，
已查看有这种遮挡时的原始场景，
该插件还提供了一个自定义pp r材质，
接受LUT调色板，
当LUT未勾选时，
QBCONFIX将调整着色器的量化模式和级别，
在朴素亮化亮度和亮度色度之间选择，
以获得更准确的结果，
这是没有量化时场景的样子，
现在让我们来摆弄一下立方贴图渲染器的参数，
层二用于从渲染中移除武器，
让我们回到原始天空，
我们也可以将LUT应用到天空材质上，
请记住，
昼夜周期会极大地影响视觉效果，
该插件还包括自定义离子如草地。
因为标准的公告牌无法与立方贴图渲染正确配合工作，
那些公告牌总是面向立方贴图中心，
而不是对其到相机平面，
我还包含了一个调色板到LUT转换器，
你可以从lose spect com下载调色板，
并将其应用到你的材质上，
下载一像素高的调色板，
在插件文件夹中找到pilot2LUT脚本，
并双击它，
为其适配一个一像素高的调色板纹理，
然后点击烘焙，
它会在与调色板相同的文件夹中生成你的LUT，
并带有后缀QBLUT，
你必须点击生成的纹理，
并将其重新导入为3D纹理，
确保相应的设置切片数量，
在这种情况下，
我使用32，
因为这是我的脚本的输出，
现在让我们将LUT应用到几何体粒子和天空上，
通过获取我的插件，
你正在支持这个频道，
以及我的游戏SPROGRAM的开发，
还有其他即将推出的游戏，
所以感谢你的观看，
希望你喜欢，
希望足够清晰，
但如果你有任何问题，
随时在评论区提问，
一如既往，
谢谢再见。


